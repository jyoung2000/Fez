"""Vendored third-party code.

Subpackages here hold model definitions / helper code we couldn't
pip-install. Each subpackage carries its own attribution and operator-
side instructions in ``infra/`` (see ``infra/saliency/README.md`` for
the TASED-Net case).
"""
