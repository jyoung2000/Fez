services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.gpu
    container_name: clipai-app
    restart: unless-stopped
    ports:
      - "1353:1353"
    volumes:
      - ./data/uploads:/data/uploads
      - ./data/outputs:/data/outputs
      - ./data/logs:/data/logs
      - ./data/fonts:/data/fonts
      # Persist user accounts + sessions + per-user settings across container
      # restarts. Without this mount the auth store (users.json, sessions.json,
      # and user_settings/*.json) lives only in the ephemeral container layer,
      # so every restart wipes the head admin's UUID and the ``owner_user_id``
      # / ``exported_by_user_id`` stamps on persisted jobs no longer resolve —
      # causing generated clips and exports to disappear from the current
      # user's view even though the underlying job files survive.
      - ./data/auth:/data/auth
      - whisper_models:/root/.cache/huggingface
      # 2026 SOTA reframing bench fixture cache. The "Run QA + SOTA
      # bench" GUI button reads MP4 fixtures + extraction caches
      # from this volume. Auto-created on first run; populate by
      # running ``bash tests/real_content/fetch.sh`` ON THE HOST
      # after editing the manifest's source_url fields. The bench
      # will report "manifest empty / no fixtures" cleanly when the
      # directory is empty - it does not crash.
      - ./data/real_content_cache:/var/cache/clipai/real_content
      # AutoFlip reference outputs — bind-mounted from host so the
      # AutoFlip sidecar (run via docker-out-of-docker) can write
      # references that this container then reads from the bench.
      - ./tests/autoflip_reference_outputs:/app/tests/autoflip_reference_outputs
      # Docker socket — required for /api/diagnostics/build-autoflip-image
      # and backend/scripts/run_autoflip_reference.py to spawn the
      # AutoFlip image build + per-clip container runs. SECURITY NOTE:
      # mounting the host's docker socket gives this container the
      # ability to spawn arbitrary host-level containers (effectively
      # root on host). For a homelab behind LAN this is the standard
      # tradeoff; for anything internet-facing, comment this out and
      # run ``make autoflip-references`` from the host shell instead.
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - PYTHONUNBUFFERED=1
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=compute,video,utility
      # Tell PyTorch to release memory back to OS aggressively.
      # Without this, torch caches 1.6GB+ VRAM after Whisper, starving Ollama.
      - PYTORCH_CUDA_ALLOC_CONF=max_split_size_mb:128,garbage_collection_threshold:0.6
      # ClipAI v4 universal reframing: enable all editorial signals by default.
      - CLIPAI_ANIME_ANCHOR=1
      - CLIPAI_MUSIC_BEAT_SNAP=1
      - CLIPAI_GAMEPLAY_TRACKER=1
      - CLIPAI_EDITORIAL_PRIOR=1
      - CLIPAI_THIRDS_BIAS=1
      - CLIPAI_GAZE_LEAD_ROOM_V2=1
      # 2026 SOTA reframing bench: where the bench finds fixture
      # MP4s + extraction cache. Maps to the volume mount above.
      - CLIPAI_REAL_CONTENT_CACHE=/var/cache/clipai/real_content
      # Host-side paths for docker-out-of-docker invocations.
      # When the app spawns ``docker run -v <path>:/data`` against
      # the AutoFlip image, the path is interpreted by the host
      # docker daemon as a host filesystem path — NOT this container's
      # mount namespace. We pass the host paths explicitly so
      # backend/scripts/run_autoflip_reference.py can translate
      # container paths to their host equivalents at -v time.
      # ``${PWD}`` resolves to the directory containing this compose
      # file when ``docker compose up`` runs.
      - CLIPAI_HOST_REAL_CONTENT_CACHE=${PWD}/data/real_content_cache
      - CLIPAI_HOST_AUTOFLIP_REF_DIR=${PWD}/tests/autoflip_reference_outputs
      - CLIPAI_HOST_AUTOFLIP_DOCKERFILE_DIR=${PWD}/infra/autoflip
    # Enable GPU passthrough when NVIDIA Container Toolkit is installed.
    # If no GPU is available, the container still starts normally on CPU.
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu, video, compute]
    env_file:
      - path: .env
        required: false
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:1353/api/jobs"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 30s
    depends_on:
      ollama:
        condition: service_healthy

  ollama:
    image: ollama/ollama:latest
    container_name: clipai-ollama
    restart: unless-stopped
    ports:
      - "11434:11434"
    volumes:
      - ollama_models:/root/.ollama
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=compute,utility
      - OLLAMA_KEEP_ALIVE=30s
      - OLLAMA_MAX_LOADED_MODELS=1
      - OLLAMA_NUM_PARALLEL=1
      # Force Ollama to use all available GPU layers
      - OLLAMA_GPU_LAYERS=-1
      # Set VRAM budget to leave headroom (GTX 1650 = 4GB, reserve 1GB for system/CUDA overhead)
      # The compute graph allocation (~300MB) must fit on top of weights + KV cache.
      # With 3.0GB budget: weights(1.8GB) + KV(38MB) + compute(300MB) = 2.14GB fits.
      - OLLAMA_MAX_VRAM=3221225472
      # Enable flash attention for memory efficiency
      - OLLAMA_FLASH_ATTENTION=1
      # Reduce KV cache to fit in VRAM
      - OLLAMA_KV_CACHE_TYPE=q8_0
      # Increase GPU discovery timeout (default 30s can fail on slow CUDA init)
      - OLLAMA_LOAD_TIMEOUT=5m
      # DO NOT set CUDA_VISIBLE_DEVICES here — it causes intermittent GPU
      # discovery failures. The NVIDIA Container Toolkit handles device
      # mapping via NVIDIA_VISIBLE_DEVICES=all + deploy.resources.
      # Help Ollama find CUDA libraries faster (prevents GPU discovery timeout)
      - LD_LIBRARY_PATH=/usr/lib/ollama/cuda_v12:/usr/local/cuda/lib64
    # Enable GPU passthrough so Ollama can use NVIDIA GPUs for inference.
    # Without this, models run on CPU (~30x slower for vision).
    # If no GPU is available, the container still starts normally on CPU.
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu, compute]
    # Add CPU quota to fix the cgroup parsing warning
    cpus: '4.0'
    healthcheck:
      test: ["CMD", "ollama", "list"]
      interval: 10s
      timeout: 10s
      retries: 8
      start_period: 45s

  # MediaPipe AutoFlip CPU sidecar for the SOTA reframing bench
  # (Task 5A). Opt-in via the "bench" profile so default-up flows do
  # not pull or build it. Writes per-clip reference JSONs into
  # tests/autoflip_reference_outputs/. See infra/autoflip/README.md.
  autoflip:
    image: clipai/autoflip:local
    container_name: clipai-autoflip
    build:
      context: ./infra/autoflip
      dockerfile: Dockerfile
    profiles: ["bench"]
    volumes:
      # Source MP4s — read-only so the binary can never modify
      # fixture inputs.
      - ./data/real_content_cache:/data:ro
      # Per-clip reference JSON + intermediate pbtxt metadata.
      - ./tests/autoflip_reference_outputs:/output


volumes:
  ollama_models:
  whisper_models:
